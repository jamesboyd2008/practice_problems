// This program demonstrates error handling.
#include <iostream>

using namespace std;

class NotADigit {}; // exception class
class OutofRange {}; // exception class

/*
- Get user input, one letter at a time (unbeknownst to them: they'll just
  be inputting a line of characters (hopefully numbers within range
  (there will be two error hangling features to ensure this))).
- throw an exception if they input something that wasn't a number.
- throw an exception if they input a number out of range.

*/
int main()
{

  return 0;
}
